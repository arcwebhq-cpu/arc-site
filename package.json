{
  "name": "arc-site",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "test": "node tests/source-contract.mjs && node tests/analytics-contract.mjs && npm run build && node tests/build-contract.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "10.7.12"
  }
}
